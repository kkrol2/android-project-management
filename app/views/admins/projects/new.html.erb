<%= form_for [:admins,@project], :html => { :multipart => true } do |f| %>
  <% if @project.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@project.errors.count, "error") %> prohibited
        this project from being saved:</h2>
    <ul>
    <% @project.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
 
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  
  <div class="field">
    <%= f.label :header_text %><br />
    <%= f.text_field :header_text %>
  </div>

  <div class="field">
    <%= f.label :link_to_app %><br />
    <%= f.text_field :link_to_app %>
  </div>
  <div class="field">
   <%= f.label :header_image_path %><br />
   <%= file_field_tag :image_path %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>